---
import { Image } from 'astro:assets';
import logoWide from '../images/logos/logo-wide.png';

const { class: className, ...props } = Astro.props;
---

<Image 
  src={logoWide} 
  alt="" 
  class={`h-auto ${className || ''}`}
  {...props}
/>
