---
---

<div id="privacy-modal" class="fixed inset-0 z-50 hidden overflow-y-auto overflow-x-hidden backdrop-blur-sm bg-white/20 dark:bg-black/20">
  <div class="flex min-h-full items-center justify-center p-4 text-center">
    <div class="relative transform overflow-hidden rounded-lg bg-white/95 p-5 text-left shadow-xl transition-all dark:bg-neutral-800/95 sm:my-8 sm:w-full sm:max-w-lg">
      <div class="absolute right-0 top-0 hidden pr-4 pt-4 sm:block">
        <button type="button" class="privacy-close-button rounded-md bg-white text-neutral-400 hover:text-neutral-500 focus:outline-none focus:ring-2 focus:ring-orange-500 focus:ring-offset-2 dark:bg-neutral-800 dark:text-neutral-300 dark:hover:text-neutral-200">
          <span class="sr-only">Cerrar</span>
          <svg class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" aria-hidden="true">
            <path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12" />
          </svg>
        </button>
      </div>
      <div class="sm:flex sm:items-start">
        <div class="mt-3 text-center sm:ml-4 sm:mt-0 sm:text-left">
          <h3 class="text-xl font-bold leading-6 text-neutral-900 dark:text-white" id="modal-title">Política de Privacidad</h3>
          <div class="mt-4 text-neutral-700 dark:text-neutral-300">
            <p class="mb-3">Nos comprometemos a proteger su privacidad:</p>
            
            <ul class="list-disc pl-5 space-y-2 mb-3">
              <li>Recopilamos información personal que usted nos proporciona directamente a través de nuestros formularios de contacto.</li>
              <li>Utilizamos esta información únicamente para proporcionar nuestros servicios y responder a sus consultas.</li>
              <li>No compartimos su información con terceros para fines de marketing.</li>
              <li>Implementamos medidas de seguridad para proteger sus datos personales.</li>
            </ul>
            
            <p class="mb-3">Usted tiene derecho a acceder, corregir o solicitar la eliminación de su información personal en cualquier momento. Para cualquier consulta relacionada con su privacidad, contáctenos a través de nuestro formulario.</p>
            
            <p class="text-sm">Última actualización: Mayo 2025</p>
          </div>
        </div>
      </div>
      <div class="mt-4 flex justify-center">
        <button type="button" class="privacy-close-button inline-flex items-center justify-center rounded-md bg-orange-500 px-4 py-2 text-sm font-semibold text-white shadow-sm hover:bg-orange-600 transition-colors duration-200">
          Cerrar
        </button>
      </div>
    </div>
  </div>
</div>

<script>
  document.addEventListener('DOMContentLoaded', () => {
    const privacyLinks = document.querySelectorAll('.privacy-link');
    const privacyModal = document.getElementById('privacy-modal');
    const privacyCloseButtons = document.querySelectorAll('.privacy-close-button');
    
    function openPrivacyModal() {
      privacyModal?.classList.remove('hidden');
      document.body.classList.add('overflow-hidden');
    }
    
    function closePrivacyModal() {
      privacyModal?.classList.add('hidden');
      document.body.classList.remove('overflow-hidden');
    }
    
    privacyLinks.forEach(link => {
      link.addEventListener('click', (e) => {
        e.preventDefault();
        openPrivacyModal();
      });
    });
    
    privacyCloseButtons.forEach(button => {
      button.addEventListener('click', closePrivacyModal);
    });
    
    // Cerrar al hacer clic fuera del contenido del modal
    privacyModal?.addEventListener('click', (e) => {
      if (e.target === privacyModal) {
        closePrivacyModal();
      }
    });
    
    // Cerrar con la tecla Escape
    document.addEventListener('keydown', (e) => {
      if (e.key === 'Escape' && privacyModal && !privacyModal.classList.contains('hidden')) {
        closePrivacyModal();
      }
    });
  });
</script>
